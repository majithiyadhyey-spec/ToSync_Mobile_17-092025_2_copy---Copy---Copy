
import React from 'react';

// This icon is no longer used and has been removed.
const ComputerDesktopIcon: React.FC<{ className?: string }> = ({ className }) => (
  null
);

export default ComputerDesktopIcon;